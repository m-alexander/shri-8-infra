test('Hello, world!', () => {
    expect('Hello, world!').toBeTruthy();
});

test('2 + 2 equal 4', () => {
    expect(2 + 2).toBe(4);
});

test('2 * 2 equal 5', () => {
    expect(2 * 2).toBe(5);
});
